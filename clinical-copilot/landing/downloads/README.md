# Clinical Co-Pilot — module downloads

This folder is generated by `clinical-copilot/landing/build.sh`. Do not edit
files here by hand; they will be overwritten on the next build.

Current build:

| File | Purpose |
|---|---|
| `oe-module-clinical-copilot-0.1.0.zip` | Versioned module zip, ready to drop into OpenEMR's `interface/modules/custom_modules/` |
| `oe-module-clinical-copilot-0.1.0.zip.sha256` | SHA-256 of the versioned zip |
| `oe-module-clinical-copilot-latest.zip` | Stable alias pointing at the latest build |
| `oe-module-clinical-copilot-latest.zip.sha256` | SHA-256 of the latest alias |

The sales site (`install.html`) links to the `latest` alias so the page
does not need editing per release. The `-0.1.0` archive is kept
alongside it for reproducibility.
